import styled from "styled-components";
import { RichText } from 'prismic-reactjs'

export const Organization = styled(RichText)`

`

export const Type = styled(RichText)`

`

export const Description = styled(RichText)`

`